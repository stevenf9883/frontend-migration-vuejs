dispatch:
  # Default service serves the typical web resources and all static resources.
  - url: "*/"
    service: default
  # Send all work to the one proxy service.
  - url: "*/api/*"
    service: proxy-service